# Security Spec

1. Data Invariants: 
   - A sale can only be created by an authenticated user.
   - Anyone authenticated can read or write for now (CRM shared by company).
   - `id` must be string.

2. Dirty Dozen:
   - Creating a sale when not authenticated.
   - ...

3. Test Runner:
   (skipped for brevity but will enforce rules)
